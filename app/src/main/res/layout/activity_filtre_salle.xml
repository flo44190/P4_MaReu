<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".controler.filtre.Filtre_Salle">

    <ImageButton
        android:id="@+id/activity_filtre_back_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_close_24px"
        android:background="@null"
        android:layout_margin="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"/>
    
    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toEndOf="@id/activity_filtre_back_btn"
        app:layout_constraintTop_toTopOf="@id/activity_filtre_back_btn"
        app:layout_constraintBottom_toBottomOf="@id/activity_filtre_back_btn"
        android:layout_margin="15dp"
        android:text="Filtrer par Salle"/>
    
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/activity_filtre_back_btn"
        app:layout_constraintBottom_toBottomOf="@id/activity_filtre_back_btn"
        android:layout_margin="16dp"
        android:text="Ok"/>

   <androidx.recyclerview.widget.RecyclerView
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       app:layout_constraintTop_toBottomOf="@id/activity_filtre_back_btn"
       android:layout_margin="16dp"
       tools:listitem="@layout/fragment_salle"/>


</androidx.constraintlayout.widget.ConstraintLayout>